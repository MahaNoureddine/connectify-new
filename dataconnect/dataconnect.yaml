specVersion: "v1beta"
serviceId: "connectify"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "connectify-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
